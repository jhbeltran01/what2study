const HOME_PAGE = "/";
const HOME_SCREEN = "/";
const REVIEWERS = "/reviewers/";
const CREATE_REVIEWER = "/reviewers/create-reviewer"; 
const STUDYPODS = "/study-pods/";
const JOIN_CALL = "/study-pods/join-call/";
const NOTES = '/notes';
const CREATE_NOTE = '/notes/create-note';

const ROUTES = {
  HOME_PAGE,
  HOME_SCREEN,
  REVIEWERS,
  CREATE_REVIEWER,
  STUDYPODS,
  JOIN_CALL,
  NOTES,
  CREATE_NOTE,
};

export { CREATE_REVIEWER, HOME_PAGE, HOME_SCREEN, JOIN_CALL, REVIEWERS, ROUTES, STUDYPODS, NOTES, CREATE_NOTE };
